# 운영 문서 — 실행 · 배포 · 검증 · 현황

전체 구조와 요약은 [SPEC.md](SPEC.md), 판단 규칙·산식 상세는 [SPEC-DETAIL.md](SPEC-DETAIL.md)에 있습니다.

## 1. 잘 만들어졌는지 확인하는 기준

| | 확인할 것 | 기준 |
| --- | --- | --- |
| ✅ | 화면이 빨리 뜨는가 (입력 있든 없든) | 5초 안에, 배경 로딩 제외 |
| ✅ | 추천 코스가 실제로 안 막히는가 | 가는 길 정체 8% 미만 |
| ✅ | 예측을 예측이라고 표시하는가 | 범위 표시, 합격 기준으로 안 씀 |
| ✅ | 후보를 제대로 만들어내는가 | 9곳 확보, 실패율 2% 미만 |
| ✅ | 대략 거리가 실제와 비슷한가 | ±15% 이내 |
| ✅ | 좋은 예측 방법이 잘 쓰이는가 | 1순위 방법 70% 이상 |
| ⚠️ | 식당 들르느라 더 막히지 않는가 | 우회 10분·정체 2%p 이하 중 **시간만** 확인 |
| ❌ | 추천 식당이 그 시각에 문을 여는가 | 카카오가 영업시간을 안 줌 ([상세 §5](SPEC-DETAIL.md#5-식당--카페-추천-선택-기능)) |
| ❌ | 리뷰·평점으로 품질을 거르는가 | 카카오가 평점·리뷰를 안 줌 ([상세 §1](SPEC-DETAIL.md#1-코스를-어떻게-고르나)) |
| ❌ | 예상 연비가 실제와 비슷한가 | 계기판 연비 입력 화면 없음 |
| ❌ | 같은 조건에 같은 결과가 나오는가 | 재현 기능 미구현 |
| ❌ | 돌아오는 시간 예측이 맞는가 | 실측 입력 없음 |

## 2. 실행과 배포

```bash
python3 server.py 4173      # 내 컴퓨터에서 → http://localhost:4173
```

- 설치·빌드 없음. 파일 더블클릭(`file://`)은 보안 정책 때문에 안 됨
- 열쇠 없이도 **가짜 데이터로 잘 동작** — 후보 생성·순위·연비 계산은 진짜로 돌고 외부 데이터만 가짜
- 진짜 데이터는 `config.local.js`(git 제외)에 → `window.SD_KEYS = { tmap: '...', kakao: '...', forceMock: false }`. 각 개발자 콘솔에 `http://localhost:4173` 등록도 필요

```bash
node build.mjs                               # "열쇠 대신 백엔드에 물어봐라"로 설정 한 줄 치환
npx wrangler deploy -c wrangler.deploy.toml   # 실제 업로드
```

- 열쇠는 Cloudflare에 암호화 저장 → 코드에 남지 않음
- 배포판 지도 배경이 바뀌는 이유 → [SPEC.md 구조](SPEC.md#배포판에-tmap-지도가-안-그려지는-이유)

## 3. 아직 안 된 것 · 남은 고민

| 구분 | 항목 | 현재 상태 |
| --- | --- | --- |
| 미구현 | AI 설명 기능 | 정해진 문장 틀로 대체 — 추천 이유가 딱딱함 |
| 미구현 | 경유지가 정체를 늘리는지 검사 | 기준값만 있고 검사 안 함 |
| 미구현 | 경유지 반영 연비 재계산 | 식당을 넣으면 연비가 실제와 어긋남 |
| 미구현 | 영업시간 확인 · 리뷰·평점 필터 | 카카오 장소 검색이 두 값을 주지 않아 실 데이터에서 무효 |
| 미구현 | 실제 주행 기록으로 보정 | 기록을 쌓는 코드가 없어 항상 "보정 없음" |
| 미구현 | 계기판 연비 입력 화면 · 재현 기능 · 설정 기억 | 없음 / 기록만 함 / 새로고침하면 초기화 |
| 고민 | 위치를 거부하면 기준점이 3곳뿐 | 실제 사용자 위치와 멀 수 있음 |
| 고민 | 장소 검색이 멈추면 추천 모드 불가 | 미리 정해둔 코스를 없앤 대가 |
| 고민 | "도로는 직선의 1.35배" · 속도 표 24개 값 | 전부 추정치. 실측·공공 데이터로 검증 필요 |
| 고민 | 기름값 가져오는 방식 | 검색 화면을 긁어와 형식이 바뀌면 조용히 실패. 공공 유가 서비스가 안전 |
| 고민 | 리뷰 신뢰도 · "분위기 좋은"의 정의 | 평점·리뷰를 주는 소스로 옮기지 않으면 품질 필터가 계속 무효 |
| 의도 | 식당 추천 기본값 꺼짐 → **켜짐** | 비동기라 화면을 늦추지 않아서 |
| 의도 | 경유지 리뷰 기준 30개 → **15개** | 목적지보다 후보가 적어 자리가 자주 비었음 |
